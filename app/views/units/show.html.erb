<p id="notice"><%= notice %></p>
<h2><%= @unit.name %></h2>
<%= link_to 'Edit', edit_unit_path(@unit) %> |
<%= link_to 'Back', units_path %>
<div class="highlight">
  <h4>Events</h4>
  <ul class="unstyled">
    <% @unit.events.each do |event| %>
      <li><%= link_to event.name, event %><br> <%= event.date_formated %></li>
    <% end  %>
  </ul>
</div>
