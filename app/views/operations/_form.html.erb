<%= form_for(@operation,:remote=>true, :html=>{class:"form-inline"}) do |f| %>
    <%= f.text_field :name, :placeholder => 'Operation Name' %>
    <%= f.collection_select :campaign_id, Campaign.all, :id, :name,prompt:'Select a campaign' %>
    <%= f.submit class:"btn" %>
<% end %>
